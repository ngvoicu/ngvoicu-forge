---
description: "Validate that existing UI code follows uispec with PASS/FAIL checks for API contracts, shapes, states, design tokens, accessibility, and form validation. Usage: /uispec-validate-ui"
allowed-tools: Read, Bash, Glob, Grep, LS
---

# UISpec Validate UI

Validate existing UI code against uispec with PASS/FAIL checks.

Invoke the uispec-frontend skill, action: **validate-ui**

Run all 6 validation checks across all endpoint implementations and report a summary with failures.
