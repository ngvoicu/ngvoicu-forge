---
description: "Scan frontend code to find gaps between UI specs and implementation: unimplemented specs, undocumented API calls, shape mismatches, design token violations, missing state handling. Usage: /uispec-detect"
allowed-tools: Read, Bash, Glob, Grep, LS
---

# UISpec Detect

Scan the frontend codebase to find gaps between UI specs and implementation.

Invoke the uispec-frontend skill, action: **detect**

Run all 5 detection checks and report findings with suggested fixes.
