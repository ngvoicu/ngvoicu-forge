---
description: "Build or update UI for a specific endpoint based on its uispec. Usage: /uispec-implement <endpoint>"
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, LS
---

# UISpec Implement

Build or update UI for endpoint: `{{ARGS}}`

Invoke the uispec-frontend skill, action: **implement**

The endpoint name is: `{{ARGS}}`

Read the endpoint spec, design system, and components docs, then generate or update the UI implementation following project conventions.
